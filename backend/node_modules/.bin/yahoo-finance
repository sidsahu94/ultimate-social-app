#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../yahoo-finance2/esm/bin/yahoo-finance.js" "$@"
else 
  exec node  "$basedir/../yahoo-finance2/esm/bin/yahoo-finance.js" "$@"
fi
